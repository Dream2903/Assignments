package com.company;

public class Terning {

    public static void main(String[] args) {

        // (1) assigning variable
        int terning = 6;

        // (2) boolean variable
        boolean t;
        t = (terning % 2 == 0) && (terning >= 3);

        // (3) print result (boolean variable)
        System.out.println(t);



    }
}
